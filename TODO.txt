ALL FILES

--investigate iteration over an enum

--Add a Sensor mode define to Config.h to change navigation modes between interrupt on change, interrupt on new reading, and instant return polling


BOX:

--Complete functionality including base classes